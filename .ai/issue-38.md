# New update search

## Задача

Добавить в модуль `src/notebook/search.py` поддержку поиска без учёта регистра.

## Контекстные файлы

- `src/notebook/search.py`
- `src/notebook/models.py`
- `tests/test_search.py`

## Требования

- функция `search_notes(notes, query)` должна искать `query` без учёта регистра
- сохранить сигнатуру функции:
  `search_notes(notes: list[Note], query: str) -> list[Note]`
- поиск должен выполняться по полям `title` и `text` объектов `Note`
- не удалять и не изменять без необходимости функцию `filter_by_tag`
- обновить `tests/test_search.py`, добавив проверки поиска без учёта регистра
- не менять остальные модули без необходимости

## Ограничения

- не заменять `Note` на строки или другие упрощённые типы
- не менять публичный API модуля
- не удалять существующие функции
- вне `src/notebook/search.py` и `tests/test_search.py` изменения допускаются только если это действительно необходимо

Generated: 2026-03-15 17:17:29.777741